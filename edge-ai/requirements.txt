# Edge-AI Module — Real-time Vision & Sensor Processing
# Runs on Raspberry Pi, Jetson, or edge Linux device

# ── Web & Communication ──────────────────────────────────────────────────────
requests==2.32.3              # HTTP client for backend API calls
websockets==14.1              # WebSocket async communication
python-dotenv==1.0.1          # Load .env environment variables

# ── Computer Vision & Image Processing ──────────────────────────────────────
opencv-contrib-python==4.10.1.26  # OpenCV for video capture and frame processing
opencv-python==4.10.1.26          # Core OpenCV library
Pillow==11.0.0                    # PIL for image manipulation

# ── Deep Learning & Object Detection ─────────────────────────────────────────
torch==2.5.1                  # PyTorch (use torch-2.x for Jetson compatibility)
torchvision==0.20.1           # Vision models from PyTorch
yolov5==7.0.13                # YOLOv5 for person/object detection
ultralytics==8.3.41           # YOLOv8 (alternative, more recent)

# ── Scientific Computing ────────────────────────────────────────────────────
numpy==2.1.3                  # Numerical computing
scipy==1.14.1                 # Scientific functions

# ── Hardware & Sensors ──────────────────────────────────────────────────────
# Note: RPi-specific packages (RPi.GPIO, Adafruit_DHT) require native installation
# Uncomment when deploying to Raspberry Pi
# RPi.GPIO==0.7.0              # GPIO control for Raspberry Pi
# Adafruit-DHT==1.2.3          # DHT22/DHT11 temperature/humidity sensor
# board==1.9.3                 # Adafruit board abstraction layer
# busio==5.2.1                 # Adafruit I2C/SPI abstraction

# ── Logging & Utilities ────────────────────────────────────────────────────
colorama==0.4.6               # Colored terminal output for logging
psutil==6.0.0                 # System monitoring (CPU, memory for heartbeat)

# ── Testing & Development ──────────────────────────────────────────────────
pytest==8.2.2                 # Unit testing framework
pytest-cov==5.0.0             # Code coverage for pytest
pytest-asyncio==0.24.0        # Async test support
mock==5.1.0                   # Mocking for unit tests
